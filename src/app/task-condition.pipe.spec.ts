import { TaskConditionPipe } from './task-condition.pipe';

describe('TaskConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
